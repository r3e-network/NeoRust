# Configure npm for better module resolution
legacy-peer-deps=false
prefer-offline=false
progress=true
audit-level=moderate

# Ensure better dependency resolution
fund=false
save-exact=false

# Configure registry and caching
registry=https://registry.npmjs.org/ 