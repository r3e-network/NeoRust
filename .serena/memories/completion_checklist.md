Before claiming completion on this project: 1) run targeted tests for the changed area; 2) run at least `cargo test --lib -- --test-threads=1 --nocapture` if core library code changed; 3) run `cargo fmt --all`; 4) if public APIs changed, update docs or plans in `docs/`; 5) report any remaining gaps between compatibility facades and the richer Rust-native API instead of implying full parity if only the facade layer was added.